import Foundation

enum NavigationRoute {
  static let providerList = "provider list"
  static let promptList = "prompt list"
  static let newPrompt = "new prompt"
  static let modelSelection = "model selection"
}

